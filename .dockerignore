# deps — ติดตั้งใหม่ใน image
node_modules
frontend/node_modules
# logworker ติดตั้ง dependency ขั้นต่ำของตัวเองไว้ที่ deploy/logworker/node_modules
# แล้ว symlink เข้า scripts/logworker/ — pattern บนสุดไม่ครอบโฟลเดอร์ซ้อน จึงต้องระบุแยก
**/node_modules

# build outputs — สร้างใหม่ตอน build image
public
dist
frontend/dist

# secrets / local
.env
.env.*
*.log

# git / tooling
.git
.gitignore
ngrok.exe

# db dumps (มี PII — ไม่ต้องเข้า image)
backup
*.dump

# ราคาจริงของบริษัท — ห้ามเข้า image (2026-09-23 · docs/plan-pricebook-db.md เฟส 0)
# image ที่ build ก่อนหน้านี้มีไฟล์ Excel ราคาอยู่ใน /app/data/ ซึ่งถูกเสิร์ฟที่ /data/ โดยไม่ตรวจสิทธิ์
# สมุดราคาอยู่ในฐานแล้ว ⇒ ไม่มีเหตุให้ไฟล์ .xlsx หรือเล่มของยุคไฟล์อยู่ในกล่องเลย
*.xlsx
**/*.xlsx
pricebook
# worktree ของ agent — สำเนาทั้งรีโปรวมไฟล์ที่ตามมาจาก data/ ของแต่ละทรี
.claude

# plan / docs ที่ไม่ต้องใน image
DEPLOY.md
# ของสำหรับรันบน host เท่านั้น (systemd unit + dependency ของ logworker) ไม่เกี่ยวกับ image
deploy

# ของทดลอง — ไม่มีอะไรในเส้นทางรันจริงเรียกใช้ ไม่ต้องแบกเข้า image
prototypes
