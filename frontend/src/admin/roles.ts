/**
 * roles — ชื่อและคำอธิบายของ role ทั้ง 5 ในภาษาที่คนอ่านบนจอ **ที่เดียวของทั้งแอดมิน**
 *
 * แยกออกมาเป็นไฟล์เมื่อ 2026-09-18 ตอนทำหน้า "สิทธิ์ตามบทบาท" — ก่อนหน้านั้นตารางนี้อยู่
 * ใน Users.tsx คนเดียว พอมีหน้าที่สองที่ต้องเรียก role ให้ตรงกัน มันจะกลายเป็นสำเนาทันที
 * และสำเนาไม่เคยพังตอนที่ก๊อป มันพังตอนที่ตัวจริงถูกแก้แล้วสำเนาไม่ได้ตามไป
 * (`docs/design.md` ข้อ 4 — ตั้งชื่อใหม่ให้ของที่มีชื่ออยู่แล้ว = สร้างของสองอย่างในหัวคนใช้)
 *
 * ⚠️ ชื่อชุดนี้เจ้าของเป็นคนเคาะเอง 2026-09-18 — **ห้ามแก้ให้ "ตรงกับชื่อ role ในโค้ด" มากขึ้น**
 *    `subadmin` เรียกว่า "ผู้เสนอราคา" และ `user` เรียกว่า "บัญชีทั่วไป" ตามที่เขาสั่ง
 *    (ของเดิมเขียนว่า "ผู้ดูแลใบเสนอราคา" กับ "ผู้ใช้ทั่วไป" ซึ่งไม่ตรงกับที่เขาเรียกจริง)
 */
import type { Role } from '../context/AuthContext';

/** ลำดับที่ใช้แสดงทุกที่ — ไล่จากสิทธิ์มากไปน้อย · ต้องตรงกับ ROLES ใน config/capabilities.ts */
export const ROLE_ORDER: Role[] = ['admin', 'approver', 'subadmin', 'salesperson', 'user'];

export const ROLE_LABEL: Record<Role, string> = {
  admin: 'ผู้ดูแลระบบ',
  approver: 'ผู้อนุมัติราคา',
  subadmin: 'ผู้เสนอราคา',
  salesperson: 'พนักงานขาย',
  user: 'บัญชีทั่วไป',
};

export const ROLE_DESCRIPTION: Record<Role, string> = {
  admin: 'จัดการได้ทุกเมนู รวมถึงผู้ใช้และสิทธิ์',
  approver: 'ทำได้ทุกอย่างของผู้เสนอราคา + อนุมัติราคาต่ำกว่าขั้นต่ำ',
  subadmin: 'ขอใบเสนอราคาและดูประวัติใบเสนอราคาของทุกคน',
  salesperson: 'ออกใบในนามตัวเอง เห็นเฉพาะใบของตัวเอง ส่งออกไฟล์ไม่ได้',
  user: 'เข้าได้เฉพาะหน้าบัญชีห้ามเสนอราคา',
};
