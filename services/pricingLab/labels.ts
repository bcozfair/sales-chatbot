// ─────────────────────────────────────────────────────────────────────────────
//  ชื่อที่โปรแกรมใช้ → ชื่อที่คนใช้
//
//  โมดูล "คิดราคาสินค้าสั่งทำ" — ถอดออกได้ทั้งก้อน ดู services/pricingLab/README.md
//
//  **ทำไมต้องมีไฟล์นี้:** คีย์อย่าง `dia_group` · `cable_extra_m` · `L1` เป็นหัวคอลัมน์ในชีต
//  Excel ซึ่งฝ่ายขายคุ้น แต่แอดมินที่ไม่เคยเปิดชีตไม่มีทางเดาได้ ⇒ ทุกอย่างที่คนเห็นบนจอ
//  ต้องผ่านตารางนี้ · ถ้าไม่มีคำไทย จะคืนคีย์เดิมกลับไป **ไม่ใช่ค่าว่าง** เพราะคำที่อ่านไม่ออก
//  ยังดีกว่าช่องว่างที่ไม่มีใครรู้ว่าหายไปตอนไหน
//
//  ⚠️ `diag:pricing` มีข้อที่ไล่คีย์ทุกตัวในสมุดราคามาเทียบกับตารางนี้ ⇒ ไฟล์ราคารอบใหม่ที่
//     มีคีย์ที่ยังไม่มีคำไทย **ด่านล้มทันที** ไม่ใช่ขึ้นจอเป็นคำเครื่องแล้วไม่มีใครสังเกต
//
//  หน้าเดโมใน prototypes/pricing/demo/labels.js มีคำชุดเดียวกันอีกสำเนา — **สำเนานั้นจะหายไป
//  พร้อมหน้าเดโม** ตอนที่โมดูลนี้แทนที่มันได้ครบ · ระหว่างนี้แก้ที่ไหนต้องแก้อีกที่ด้วย
// ─────────────────────────────────────────────────────────────────────────────

/** แกน = ตัวเลือกแบบจำกัด มาจากหัวแถว/หัวคอลัมน์ของตารางราคาในชีต */
export const AXIS_TH: Record<string, string> = {
  D: 'ขนาดแกน',
  thread: 'ขนาดเกลียว',
  cable: 'ชนิดสาย',
  sensor: 'ชนิดเซนเซอร์',
  dia_group: 'กลุ่มขนาดท่อเซรามิก',
  flange: 'ขนาดหน้าแปลน',
};

/** ช่องตัวเลข = ปริมาณที่คนกรอกเป็นตัวเลข (หรือระบบคำนวณให้จากตัวอื่น) */
export const DIM_TH: Record<string, string> = {
  L1: 'ความยาวช่วงแรก',
  L2: 'ความยาวช่วงที่สอง',
  L_total: 'ความยาวรวม',
  cable_m: 'ความยาวสาย',
  cable_cm: 'ความยาวสาย (ซม.)',
  cable_extra_m: 'ความยาวสายส่วนที่เกิน',
  teflon_mm: 'ความยาวปลอกเทฟลอน',
  hold_od_mm: 'ขนาดนอกของตัวจับยึด',
  watt: 'กำลังไฟ',
  // Band Heater (BH) ไม่ได้คิดตามความยาว แต่คิดตาม "พื้นที่แผ่น" เป็นตารางนิ้ว แล้วเปิดตารางช่วงราคา
  area_in2: 'พื้นที่แผ่น (ตร.นิ้ว)',
};

/** ชนิดของกฎบวกเพิ่ม — คำเดียวกับที่ปรากฏในไฟล์ .xlsx ที่ส่งออกไปแก้ */
export const KIND_TH: Record<string, string> = {
  base: 'ราคาตั้ง',
  flat: 'บวกเงินคงที่',
  percent: 'บวกเปอร์เซ็นต์',
  perUnit: 'บวกตามส่วนที่เกิน',
  basePrice: 'ราคาตั้งต้นของตัวเอง',
  setAxis: 'ตั้งค่าให้ช่อง',
};

export const axisLabel = (key: string): string => AXIS_TH[key] ?? key;
export const dimLabel = (key: string): string => DIM_TH[key] ?? key;
export const kindLabel = (key: string): string => KIND_TH[key] ?? key;
