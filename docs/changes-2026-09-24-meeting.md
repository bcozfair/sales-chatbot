# การเปลี่ยนแปลงตามความต้องการใหม่หลังประชุม 24/9/2026 — หน้า "ขอใบเสนอราคา"

สถานะ (2026-09-25): ข้อ 1–12 **merge เข้า main และ push แล้ว ดูได้ที่พรีวิว 5180 · ยังไม่ deploy**
(เจ้าของ deploy เอง)

ขอบเขต: เฉพาะงานหน้าขอใบเสนอราคาที่สั่งหลังประชุม · งานสมุดราคา/รหัส TS ที่ทำคู่ขนานในช่วงเดียวกัน
ไม่นับรวม (อยู่ที่ `docs/pricing-code-ts-01.md`)

## สรุปรายการ

| # | ความต้องการ | สิ่งที่เปลี่ยน | คอมมิต |
| --- | --- | --- | --- |
| 1 | จำไม่ได้ว่าลูกค้ารายไหนเป็นของเซลส์คนไหน | ช่อง "ออกในนาม" เริ่มว่าง แล้วเติมเซลส์ของบริษัทให้เมื่อรู้ลูกค้า · เซลส์ไม่ active ถอยหาคนอื่นใน `company_id` เดียวกัน 4 ขั้น · ป้าย "ระบบเลือก" / "เลือกเอง" · หาไม่เจอ = ให้คนเลือก ไม่เดา · แก้ใบเดิม = เซลส์ของใบเดิม | `6531bd9` `4481d6b` `7be46fb` |
| 2 | อยากกลับไปให้ระบบเลือกหลังเลือกเองไปแล้ว | แถว "ระบบเลือกอัตโนมัติ" บนสุดของรายการ "ออกในนาม" | `0d35a05` |
| 3 | ค่าบริการที่กดเพิ่มเองไม่ควรใช้ชื่อของกฎค่าขนส่ง | ตั้งต้นเป็น "ค่าบริการ" · เลือกชื่อจากรายการ (ค่าบริการส่งสินค้ากรณียอดไม่ถึง 1,000 บาท · ค่าเปลี่ยนแปลงสินค้า · Deployment Package) หรือพิมพ์เอง | `b10595a` |
| 4 | ค่าขนส่งที่ระบบเติมให้ต้องแก้ได้ | แก้ชื่อ/ราคาทับได้ + ปุ่มคืนค่าจากหน้าตั้งค่า · ยังเป็นบรรทัดของกฎ (ยอดถึงเกณฑ์/ลูกค้าเครดิต = หายเองเหมือนเดิม) | `cc65ad7` |
| 5 | ข้อมูลลูกค้าควรขึ้นตั้งแต่เลือกบริษัท | หัวใบขึ้นรหัสลูกค้า/เลขภาษี/เครดิต/ที่อยู่ทันทีที่เลือกบริษัท · บริษัทที่มีผู้ติดต่อคนเดียวเลือกให้เลย ยกเว้นข้อความระบุชื่อมาแล้วไม่ตรง และยกเว้นตอนเปิดใบเดิม | `473237b` |
| 6 | ตัวเลขในแถวสูงต่ำไม่เท่ากัน · ช่องชื่อค่าบริการแคบ | จำนวน/ราคา/ส่วนลด/ยอด อยู่แนวเดียวกับช่องกรอกทุกแถว · ช่องชื่อค่าบริการกว้างเต็มคอลัมน์ "รายการ" | `af8fc72` |
| 7 | แสดงส่วนลด 3 บิลล่าสุดของลูกค้าในหน้าเสนอราคา | เจ้าของเลือกแบบ C จาก mockup 3 แบบ แล้วสั่งวางใต้ "สถานที่ส่งของ": แถว "ส่วนลดเดิม" ปุ่ม `30%, 30%, 25% ▾` กดแล้วกางตาราง 3 ใบ (เลขที่ใบ · วันที่ · ยอดก่อนลด · %) · ข้อมูลชุดเดียวกับหน้า "ข้อมูลลูกค้า" นับเฉพาะรหัสลูกค้านี้ · ไม่พิมพ์ลง PDF · API ใหม่ `GET /api/admin/webquote/discount-history` (`quote.create`) | `e26964c` |
| 8 | ใบแยก PM/THT ไม่ต้องแสดงข้อมูลลูกค้าซ้ำ | บล็อกผู้ซื้อมีชุดเดียวที่ใบแรก · ใบถัดไปเหลือบรรทัด "นามผู้ซื้อ · ข้อมูลลูกค้าและผู้ติดต่อเดียวกับใบ PM ด้านบน" · PDF ของทุกใบยังมีผู้ซื้อครบ | `e26964c` |
| 9 | ช่อง Term Payment บอกให้ชัดว่ายังเป็นค่าของลูกค้า | คำว่า "(ของลูกค้า)" ท้ายค่า เปลี่ยนเป็นป้ายเขียวสีแบรนด์ "ค่าเดิม" ในกรอบช่อง (ภาษาเดียวกับป้าย "ส่วนลดเดิม") · ป้ายหายเมื่อตั้งเครดิตเอง (ขอบน้ำเงิน + บรรทัด ✏️ บอกแทน) · ค่าเดียวกับของลูกค้าไม่ขึ้นซ้ำในรายการตัวเลือก | `3dc8d29` |
| 10 | ลูกค้าเครดิตที่ไม่มีบิลเกิน 12 เดือน → Term Payment เป็น Cash อัตโนมัติ + แจ้งเตือน | เงื่อนไขเดียวกับด่านเครดิตเดิม (`checkCreditHold` · เกณฑ์เดือนตั้งที่หน้า "เกณฑ์เครดิต") · ตั้งให้ **ครั้งเดียวตอนเลือกบริษัท** · ช่องขอบเหลือง ป้าย "ระบบตั้ง" + บรรทัดเหตุผล/บิลล่าสุด/เครดิตเดิม (mockup แบบ A) · กด ↺ กลับเป็นเครดิตได้ ระบบไม่ตั้งกลับเอง มีปุ่ม "ใช้ Cash" · ใบที่โหลดกลับมาแก้คงค่าของใบต้นทาง · บัญชีที่ตั้งเครดิตทับไม่ได้ (เซลส์ ตามค่าตั้งต้น) เห็นแค่คำเตือน · **เจ้าของเลือก: ด่านเครดิตยังบล็อก/ติ๊กรับทราบตาม role เหมือนเดิม · ใบที่เป็น Cash เข้าคิว "ต้องแก้มือใน Odoo"** · API: ก้อนลูกค้าของ `/party`/`/preview` มี `credit_hold` · gate: `npm run diag:web-credit-dormant` (อ่านอย่างเดียว) | `5f96c6a` |
| 11 | ลดคำเตือนซ้ำ + modal ยืนยันกระชับ | กล่องบนจอ "ติดด่านตรวจ" ไม่ขึ้นข้อด่านเครดิต (เมื่อแถว Term Payment เตือนอยู่แล้ว) และกล่อง "ต้องแก้มือใน Odoo" ไม่ขึ้นเรื่องเครดิตที่ตั้งทับ — กล่องที่ว่างแล้วหายทั้งกล่อง · ทั้งสองเรื่องยังขึ้นครบใน modal ยืนยัน · modal: ช่องติ๊ก "ข้าพเจ้าอ่านครบแล้ว และยืนยันข้ามกฎ N ข้อ" อยู่แถวเดียวกับปุ่ม · ปุ่ม "กลับไปแก้/ยืนยันออกใบ" → "แก้ไข/ยืนยัน" (กรณีขออนุมัติราคาปุ่มยังเป็น "ส่งขออนุมัติราคา") · เหตุผลปุ่มยืนยันจางเมื่อยังไม่มี "ออกในนาม" → "กรุณากรอกข้อมูลครบก่อน" | `0f76be8` |
| 12 | คำเตือนกฎแบบ A: ป้ายที่ต้นเหตุ + ยืนยันครั้งเดียว (เลือกจาก mockup 3 แบบ) | **แยกตามว่า role ข้ามได้ไหม** (อ่าน `blocked_keys` ของ server) · **ข้ามได้** (admin/subadmin/approver): ไม่มีกล่องแดงเหนือใบ — สินค้าติดป้ายที่แถว · ด่านเครดิตอยู่แถว Term Payment · บัญชีห้ามเสนอราคาเป็นป้ายใต้ชื่อบริษัท · ป้าย "ข้ามกฎ N ข้อ" ข้างปุ่มยืนยัน · modal เหลือหนึ่งบรรทัดต่อข้อ (ลูกค้า/สินค้า) ด้วยถ้อยคำสั้นของหน้าเว็บ + บรรทัดคิวแก้มือ Odoo + ข้อเท็จจริงของการกดบรรทัดเดียว · **ข้ามไม่ได้** (เซลส์): กล่องแดงเดียว "ออกใบไม่ได้ — ติด N ข้อ · แก้ใบ หรือให้แอดมินออกใบแทน" ปุ่มจาง ไม่มี modal — **แก้บั๊กเดิม** ที่เซลส์ติ๊กได้ กดได้ แล้วค่อยโดนปฏิเสธ · ข้อที่ไม่มีที่ลงบนใบยังขึ้นกล่องเหนือใบ · ถ้อยคำของ LINE ไม่เปลี่ยน | (คอมมิตนี้) |

## สิ่งที่ต้องรู้ก่อน deploy

- ข้อ 1 มี migration `migrations/changes/2026-09-24_01_customers_data_salesperson_id.sql` (เพิ่ม `customers_data_view.salesperson_id`
  ท้ายสุดของ view) — **รันบนฐาน PMSV แล้ว 2026-09-24** (สำรองก่อนที่
  `backup/auto_chatbot_primus_2026-09-24_1558.dump`) · ฐานอื่นต้องรัน + refresh แบบ `force` ตาม `DEPLOY.md` ขั้น 4
- ข้อ 2–8 เป็นโค้ดล้วน ไม่มี migration
- ใบที่ออกจาก LINE ไม่เปลี่ยน — ทุกข้อแตะเฉพาะหน้าเว็บ (ข้อ 5 ใช้ `findContactCandidates` ตัวเดียวกับ LINE ไม่ได้ก๊อป)

## ด่านที่ใช้พิสูจน์

`npx tsc --noEmit` · `npm --prefix frontend run lint` · `npm --prefix frontend run build` ·
`diag:web-sales-owner` · `diag:web-quote` · `diag:role-permissions` · `diag:local-contacts` ·
ตัวเปิดหน้าจริงกดจริงที่ 1280/390px: `webSalesOwnerUiProbe` · `webServiceNameUiProbe` ·
`webAutoFeeUiProbe` · `webCompanyHeaderProbe` · `webDiscountHistoryProbe` (ทั้งหมดอยู่ใน `scripts/diag/`)

รายละเอียดเหตุผลของข้อ 1–2: `docs/plan-web-quote-auto-salesperson.md`
